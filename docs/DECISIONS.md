# ทำไมถึงทำแบบนี้ — อ่านก่อนคิดจะเปลี่ยน

เก็บเฉพาะข้อที่ "ดูแปลก แต่มีเหตุผล" ถ้าไม่มีบันทึกไว้ คนถัดไปจะแก้กลับแล้วพังซ้ำที่เดิม

---

## D1 · แบบทดสอบใช้ "กิจกรรมในงาน 41 มิติ" ไม่ใช่ RIASEC

RIASEC (Realistic/Investigative/Artistic/Social/Enterprising/Conventional) เป็นมาตรฐานที่คนใช้กันมากที่สุด
เราลองแล้วและ **ใช้ไม่ได้กับโจทย์ของเรา** — วัดกับ 8 อาชีพเป้าหมายจริง:

| เครื่องมือ | มิติ | คู่ที่แยกยากที่สุด |
|---|--:|--:|
| **กิจกรรมในงาน** | 41 | **2.29** 🟢 |
| RIASEC | 6 | 0.43 🔴 หุ่นยนต์กับกระบวนการผลิตแทบทับกัน |
| ค่านิยมในการทำงาน | 6 | 0.69 🔴 |
| บุคลิกในการทำงาน | 16 | 0.91 🔴 |

RIASEC ทำให้วิศวะทุกสายกองรวมกันที่ Realistic+Investigative
ซึ่งคือปัญหา *"แบบทดสอบแนะนำให้เป็นพยาบาลทุกคน"* ในเวอร์ชันของเรา

ตัวเลขนี้สร้างใหม่ได้ตลอด: `python pipeline/1b_import_instruments.py` → `pipeline/out/discrimination.json`

---

## D2 · ให้คะแนนด้วยโปรไฟล์ดิบ ไม่ใช่โปรไฟล์ที่หักค่าเฉลี่ยแล้ว

เคยลองหักค่าเฉลี่ยของทุกอาชีพออกเพื่อให้คะแนนกระจายมากขึ้น ผลคือ:

| น้ำหนักที่หัก | เทสต์กู้คืน | ช่องว่างอันดับ 1–2 |
|--:|--:|--:|
| 0.0 | **8/8** ✅ | 0.035 |
| 0.2 | 5/8 ❌ | 0.049 |
| 1.0 | 5/8 ❌ | 0.092 |

คะแนนกระจายขึ้นก็จริง แต่ทิ้งข้อมูลที่อาชีพกลาง ๆ ใช้แยกตัวเอง จนกู้คืนไม่ได้
**สรุป: ให้คะแนนด้วยโปรไฟล์ดิบ · ใช้ค่าที่หักเฉลี่ยแล้วเฉพาะตอนเลือกคำอธิบายว่าอะไรเด่นของอาชีพนี้**

---

## D3 · เหตุผลที่ขึ้นหน้าจอ ไม่ใช่ทุกข้อที่ดันคะแนน

ตอนแรกแสดงข้อที่มีน้ำหนักมากที่สุดตรง ๆ ผลคือระบบบอกว่า
*"เสนอวิศวกรข้อมูล เพราะคุณไม่อยากซ่อมเครื่องจักร"* — จริงทางคณิตศาสตร์ แต่อ่านแล้วเหมือนระบบมั่ว

ตอนนี้ `TraceEntry.direction` แยก 4 กรณี และ `headline_reasons` เอาขึ้นจอเฉพาะสองกรณีที่อ่านแล้วเข้าใจ:
- `wants_and_does` คุณอยากทำ และงานนี้ได้ทำเยอะ
- `unwanted_but_core` คุณไม่อยากทำ แต่งานนี้ต้องทำเยอะ

---

## D4 · ตัวสกัด CV เป็นการจับคำสำคัญ ไม่ใช่ LLM

**ไม่ใช่เพราะขี้เกียจ — เพราะยังไม่มี API key** และเราไม่อยากให้ทั้งระบบสร้างไม่ได้เพราะรอ key

โครงแยกเป็น `app/llm/base.py` (สัญญา) · `keyword.py` (ค่าเริ่มต้น) · `anthropic.py` (รอ key)
สลับด้วย env ตัวเดียว ไม่ต้องแก้ logic ที่อื่นเลย

**span guard ทำงานเหมือนกันทั้งสองตัว** — ผู้ใช้จึงยืนยัน/แก้ผลได้อย่างมีความหมายตั้งแต่ตอนนี้

ข้อจำกัดที่ต้องบอกตามตรง: จับได้เฉพาะคำที่เขียนตรงตัว
*"ทำให้สองระบบคุยกัน"* จับไม่ได้ ต้องเขียนว่า *"API"*

---

## D5 · ทักษะจาก CV กับที่กรอกเอง เป็นคนละตาราง และจะเป็นคนละตารางตลอดไป

`extracted_skill` มี span ชี้กลับไปที่บรรทัดใน CV · `self_reported_skill` ไม่มีหลักฐาน

roadmap ใช้ทั้งสองในการคำนวณ แต่ **หน้าจอต้องแยกให้เห็นเสมอ**
ถ้ารวมสองอย่างนี้เป็นก้อนเดียวเมื่อไหร่ เราก็กลายเป็นเว็บหางานธรรมดาที่เชื่อสิ่งที่คนกรอกทันที

---

## D6 · เงื่อนไข "ถาวร" กับ "ตามเวลา" กรองคนละแบบ

- **ถาวร** (สาขาที่เรียน · เงื่อนไขชดใช้ทุน) → ตัดอาชีพออกจริง **พร้อมแสดงเหตุผล**
- **ตามเวลา** (ชั้นปี · เกรด) → **ไม่ตัด** แสดงเป็น "สมัครได้เมื่อถึงปี 3"

ถ้ากรองแข็งทั้งหมด นักศึกษาปี 1 จะเห็นรายการว่างเปล่า
ซึ่งพังตรงแก่นของผลิตภัณฑ์ที่ทั้งอันคือ *การแสดงเส้นทางไปยังอนาคต*

---

## D7 · บางทักษะ "ทำเมื่อไหร่ก็ได้" ไม่บล็อกก้าวอื่น

กลไกนี้ยกมาจาก roadmap.sh (*"Order not strict / Learn anytime"*)

`ORDER_FLEXIBLE` = อ่านเอกสารอังกฤษ · เขียนเอกสารเทคนิค · ทำเอกสารโครงการ · นำเสนอ · ทำงานเป็นทีม
ทักษะพวกนี้พัฒนาไปเรื่อย ๆ ตลอดหลักสูตร ไม่ใช่ประตูที่ต้องผ่านก่อน
ไม่ได้แปลว่าไม่สำคัญ — แปลว่าไม่ต้องรอให้เสร็จก่อนถึงจะเริ่มอย่างอื่น

---

## D8 · ก้าวที่มีหลักฐานบางส่วนแล้ว ไม่ขึ้นแม่กุญแจ

สถานะ `in_progress` แยกออกมาจาก `locked`
เพราะตอนแรกหน้าจอขึ้น *"มีหลักฐานจาก CV"* พร้อมกับ 🔒 ซึ่งอ่านแล้วขัดกันเอง

---

## D9 · โครงทักษะอ้างอิง O*NET และตรวจสอบได้

73 ทักษะที่ใช้จริงเป็นชั้นที่ **คนคัดจาก 664 ตัวที่ O*NET ให้มา** แล้วตั้งชื่อไทย

`skill.onet_element_id` 47 ตัวชี้กลับไปที่ O*NET ได้ และ **มีเทสต์ตรวจว่ามีอยู่จริง**
— ทำให้ข้ออ้างว่า "อิงมาตรฐานสากล" พิสูจน์ได้ ไม่ใช่แค่พูด

ที่เหลือติดป้าย `source: market` หรือ `manual` ตามความจริง

---

## D10 · สองอาชีพใช้โปรไฟล์กิจกรรมของอาชีพอื่น และบันทึกไว้

O*NET ยังไม่มีข้อมูลกิจกรรมของ Software Developers (15-1252) และ Data Scientists (15-2051)
จึงใช้อาชีพในตระกูลเดียวกันที่มีข้อมูลครบแทน:

| อาชีพของเรา | ใช้โปรไฟล์ของ |
|---|---|
| วิศวกรซอฟต์แวร์ | Computer Programmers (15-1251) |
| วิศวกรข้อมูล | Business Intelligence Analysts (15-2051.01) |

บันทึกไว้ใน `careers.py` ฟิลด์ `activity_proxy_note` และมีเทสต์บังคับว่า
**ถ้าใช้ตัวแทนแล้วไม่บันทึก เทสต์จะแดง** — ห้ามใช้เงียบ ๆ

---

## D11 · ประกาศงานเก็บด้วยมือ ไม่เขียน scraper

30–50 ประกาศ copy-paste ลงไฟล์พร้อม URL/วันที่/องค์กร
ไม่ผิด ToS · ไม่ต้องสร้าง infra · ไม่พังตอนเว็บต้นทางเปลี่ยนโครง · ปริมาณเท่านี้ทำเสร็จใน 1 วัน

---

## D12 · draft 1 ยังอยู่ที่ `../prototype/` และไม่ใช่ของที่ใช้แล้ว

เป็นผลิตภัณฑ์คนละตัว (เล็งคนที่ *ไม่รู้ว่าอยากได้อะไร* แล้วให้เลือกจากคลังผลงาน**คนอื่น** โดยจงใจไม่ใช้ AI)
เก็บไว้อ้างอิงเฉย ๆ · เครื่องยนต์บางส่วนถูกยกมาใช้ต่อแล้ว (`skill_graph.py` · `eligibility.py`)
**อย่าเอาโค้ดหรือแนวคิดจากตรงนั้นมาใช้เพิ่มโดยไม่ถาม**

---

## D13 · LLM ในเครื่องใช้ `gemma4:26b` — เลือกจาก "ไม่ตีความเกินหลักฐาน" ไม่ใช่ "เจอเยอะ"

วัดกับ CV ตัวอย่างเดียวกันบนเครื่องเดียวกัน ผ่าน Ollama:

| รุ่น | เวลา | ทักษะ | ประโยคที่อ้าง | **ต่อประโยค** | จับประโยคที่เขียนอ้อม |
|---|--:|--:|--:|--:|:--:|
| llama3:8b | 10.6s | 4 | 4 | 1.0 | ✗ |
| qwen2.5:14b | 78.1s | **19** | 10 | **1.9** 🔴 | ✗ |
| **gemma4:26b** | 77.9s | 12 | 12 | **1.0** ✓ | **✓** |
| qwen3.6:35b-A3B | 157.9s | 10 | 9 | 1.11 | ✗ |

**"ต่อประโยค" คือตัวชี้ขาด ไม่ใช่จำนวนทักษะ** — qwen2.5 ได้เยอะที่สุดเพราะเอา
ประโยคเดียวไปอ้างเป็นหลักฐานของหลายทักษะ ที่เจอจริงคือประโยค
*"ทำระบบวิเคราะห์ข้อมูลการใช้ห้องเรียนด้วย Python และ pandas"* ถูกอ้างเป็นหลักฐานของ
5 ทักษะ รวมถึง *"จับเวลาการทำงานและหาว่าขั้นตอนไหนเป็นคอขวด"* ซึ่งประโยคนั้นไม่ได้บอก

**span guard จับกรณีนี้ไม่ได้** เพราะข้อความมีอยู่จริงในเอกสาร แค่ไม่ได้แปลว่าอย่างที่โมเดลอ้าง
— นี่คือเหตุผลที่กติกา "ผู้ใช้ต้องยืนยันก่อนนับ" สำคัญกว่าที่คิด และห้ามตัดออก

`gemma4:26b` เป็นรุ่นเดียวที่จับประโยคเขียนอ้อมได้ (*"เขียนสคริปต์ให้เครื่องทำงานซ้ำ ๆ
แทนคนทุกเช้า"*) โดยคัดลอกมาตรงตัวจนผ่าน guard — ซึ่งเป็นเหตุผลทั้งหมดที่จะใช้ LLM แทน keyword

วัดซ้ำได้: `make try-extractor P=local M=<รุ่น> CMP=1`

⚠️ **78 วินาทีต่อ CV 1 ใบ ช้าเกินสาธิตสด** — วันสาธิตให้ใช้ `keyword` แล้วโชว์ผลของ
`gemma4` เป็นภาพเทียบที่เตรียมไว้ล่วงหน้า

### สามบั๊กที่เจอระหว่างวัด และแก้แล้ว

1. **`parse_payload` ทิ้งทั้งแถวเมื่อ `confidence` เป็นข้อความ** — gemma4 ตอบ `"high"`
   ทำให้ 15 แถวที่ถูกต้องทุกอย่างถูกทิ้งหมด · แก้เป็นแปลคำเป็นตัวเลข
   หลักฐานที่แท้จริงคือ `span_text` ไม่ใช่ตัวเลขระดับ
2. **`max_tokens=4000` น้อยเกินสำหรับ reasoning model** — gemma4 กับ qwen3 เขียนส่วนคิด
   7,000–8,400 ตัวอักษรก่อนตอบ และโควตาแชร์กัน · ฝั่ง local ขึ้นเป็น 16,000
3. **ข้อความ error บอกผิดสาเหตุ** — timeout ขึ้นว่า "ต่อเซิร์ฟเวอร์ไม่ได้" ทั้งที่เซิร์ฟเวอร์รันอยู่

---

## D14 · ห้ามเขียน "เหมาะกับคุณ 95%" — ตัวเลขที่มีคือ *อันดับ* ไม่ใช่ *ความเหมาะสม*

ดีไซน์ที่ทีมส่งมาให้การ์ดอาชีพติดป้าย `95% Match` · `88% Match` · `82% Match`
ตัวเลขพวกนี้ **ไม่มีอยู่จริงในระบบ** และไม่มีวันมี

`relative_score` ที่ API ส่งคือ *ตำแหน่งเทียบกันในกลุ่มอาชีพที่นำมาเทียบ* — อันดับ 1
ได้ 100 เสมอเพราะเป็นอันดับ 1 ไม่ได้แปลว่าเข้ากัน 100% · คะแนนดิบไม่เคยถูกส่งออกไปเลย
เพราะมันกองกันที่ 0.88–0.96 (ดู `api_discover.py` หัวไฟล์)

**ที่ตกลงกัน**

| เขียนแบบนี้ไม่ได้ | เขียนแบบนี้ได้ |
|---|---|
| เหมาะกับคุณ 95% | อันดับ 1 จาก 8 อาชีพที่เทียบ |
| 88% Match | อันดับ 2 · ตามหลังอันดับ 1 อยู่พอสมควร |
| ความแม่นยำ 82% | อยู่ในกลุ่มที่เป็นไปได้ |

แถบยาว-สั้นเทียบกันใช้ได้ (มันสื่อ "เรียงกันยังไง" ซึ่งจริง) แต่ห้ามมีเครื่องหมาย `%`
และต้องมี `scale_note` อยู่ในสายตาเสมอ

`/discover/result` และ `interim` เพิ่มฟิลด์ `compared_count` ให้แล้ว — หน้าจอจะได้เขียน
"จาก 8 อาชีพ" ได้โดยไม่ต้อง hardcode เลข 8 ซึ่งจะผิดทันทีที่เพิ่มอาชีพที่ 9

---

## D15 · แบบทดสอบใช้สเกล 5 ระดับ ไม่ใช่ 3 — วัดแล้วว่า 3 ระดับกู้อาชีพกลาง ๆ ไม่ได้

เดิมเป็น 3 ตัวเลือก (อยากทำ · เฉย ๆ · ไม่อยากทำ) ดีไซน์วาดมาเป็น 5 ระดับ
เราคิดจะให้ดีไซน์ลดเหลือ 3 เพื่อไม่ต้องแตะเครื่องยนต์ **แล้ววัดก่อน — ผลกลับด้าน**

จำลองผู้ใช้ที่ "ควรได้อาชีพ X" ทั้ง 8 อาชีพ × 20 seed เดินแบบทดสอบตามลำดับจริงที่
`/discover/next` ใช้:

| สัญญาณรบกวน | สเกล | กู้คืนถูก | ข้อเฉลี่ย | ช่องว่างอันดับ 1-2 |
|---|:--:|--:|--:|--:|
| ไม่มี | 3 ระดับ | 88% | 16.0 | 0.095 |
| ไม่มี | **5 ระดับ** | **100%** | **12.0** | **0.129** |
| sd 0.4 | 3 ระดับ | 81% | 15.4 | 0.100 |
| sd 0.4 | **5 ระดับ** | **96%** | **13.4** | **0.128** |
| sd 0.8 | 3 ระดับ | 62% | 14.3 | 0.116 |
| sd 0.8 | **5 ระดับ** | **78%** | **13.8** | **0.136** |

**ที่แพ้ไม่ใช่แพ้เท่า ๆ กันทุกอาชีพ** — 3 ระดับพังเฉพาะอาชีพกลาง ๆ ที่ D1 บอกว่าแยกยากอยู่แล้ว
(วิศวกรข้อมูล 12/20 · กระบวนการผลิต 11/20 · หุ่นยนต์ 13/20) พอเป็น 5 ระดับกลายเป็น
20/20 · 19/20 · 19/20 ส่วนอาชีพที่มีลายเซ็นชัดอยู่แล้ว (ซอฟต์แวร์ · โครงสร้าง · ไฟฟ้ากำลัง)
ได้ 20/20 ทั้งสองสเกล

**ทำไมถึงเป็นแบบนั้น** `_activity_score` ใช้สหสัมพันธ์ ซึ่งไม่สนสเกลรวมแต่สนว่า
*ข้อไหนแรงกว่าข้อไหน* · ตอบ ±1 ล้วนแปลว่าทุกข้อที่ไม่ใช่ "เฉย ๆ" มีน้ำหนักเท่ากันหมด
ข้อมูลว่า "ข้อนี้อยากทำมากกว่าข้ออื่น" หายไปทั้งหมด ซึ่งคือข้อมูลที่ใช้แยกอาชีพที่ใกล้กัน

⚠️ **ข้อจำกัดของตัวเลขชุดนี้** ผู้ใช้จำลองมีความชอบเท่ากับลายเซ็นของอาชีพนั้นพอดี
คนจริงไม่เป็นแบบนั้น — ตัวเลขที่ได้เป็นขอบบน ไม่ใช่ค่าที่จะเจอจริง แต่เทียบสองสเกลกันได้
เพราะทั้งคู่เจอสมมติฐานเดียวกัน

วัดซ้ำได้: `make answer-scale`

ต้องแก้ตามด้วย: `AnswerRequest.answer` เป็น `Literal[-2..2]` · `ANSWER_CHOICES` 5 ข้อ
· `/discover/next` ส่ง `answer_choices` มาให้หน้าจอ **ห้าม hardcode ป้ายเอง**
ถ้อยคำเป็น "อยากทำ" ไม่ใช่ "ชอบ" เพราะคำถามถามถึงความอยากลงมือทำ ไม่ได้ถามความชอบ

---

## D16 · ไม่มี "ข้อที่ 1 จาก 41" — ความยาวขึ้นกับว่าแยกอาชีพออกเมื่อไหร่

ดีไซน์เขียนว่า `QUESTION 1 OF 41` ซึ่งขัดกับสิ่งที่ระบบทำจริง — แบบทดสอบเลือกข้อถัดไป
จากอันดับ 1 กับ 2 ปัจจุบัน และจบเมื่อแยกออก อยู่ระหว่าง 12–24 ข้อ ไม่เคยถาม 41 ข้อ

การเขียน "จาก 41" ไม่ใช่แค่ผิด — มันทิ้งจุดขายไปด้วย เพราะสิ่งที่ทำให้เราต่างจาก
แบบทดสอบทั่วไปคือ *ระบบบอกได้ว่าทำไมยังถามต่อ* (`reason` เช่น "ตอนนี้ วิศวกรข้อมูล
กับ วิศวกรซอฟต์แวร์ ยังแยกกันไม่ชัด ขอถามอีก 4 ข้อ")

**ที่ตกลงกัน** แถบความคืบหน้าใช้ `answered / max_items` เป็นตัวเติม และทำเครื่องหมาย
ที่ `min_items` ว่า "ถึงตรงนี้ดูผลได้แล้ว" · `can_finish` (เพิ่มใน `/discover/next` แล้ว)
เป็นตัวสั่งว่าปุ่ม "ดูผลเลย" ขึ้นได้หรือยัง · `reason` ต้องขึ้นจอทุกข้อ ไม่ใช่ซ่อนไว้

---

## D17 · Gemini ใช้ `gemini-3.1-flash-lite` — เลือกจาก "ให้ผลเดิมทุกครั้ง" ไม่ใช่ "เร็วที่สุด"

**ทำไมต้องมี provider นี้ทั้งที่มี `local` แล้ว — เรื่องความเร็ว ไม่ใช่ความแม่น**
D13 วัดไว้ว่า LLM ในเครื่องใช้ ~78 วินาทีต่อ CV 1 ใบ ช้าเกินสาธิตสด แผนเดิมจึงเป็น
"วันสาธิตใช้ keyword แล้วโชว์ผล LLM เป็นภาพที่เตรียมไว้" — ชั้นฟรีของ Google ตอบใน
~4 วินาที ทำให้**สาธิต LLM จริงสดบนเวทีได้**

วัดกับ CV ตัวอย่างใน `scripts/try_extractor.py` (581 ตัวอักษร) · `temperature=0` ทุกตัว
· **รันตัวละ 3 ครั้ง** เพราะรันครั้งเดียวไม่พอตัดสิน

| รุ่น | เวลา | ทักษะ (3 ครั้ง) | ต่อประโยค | จับประโยคอ้อม | **ผลซ้ำเดิมไหม** |
|---|--:|--:|--:|:--:|:--:|
| keyword | 0.0s | 11 · 11 · 11 | 1.00 | 0/2 | ✓ |
| **gemini-3.1-flash-lite** | **3.8–5.5s** | **11 · 11 · 11** | **1.00** | **1/2** | **✓ เหมือนกันเป๊ะ** |
| gemini-3.5-flash-lite | 2.1–2.5s | 13 · 14 · 12 | 1.00–1.17 | 0–2/2 | 🔴 ไม่ซ้ำเลยสักครั้ง |
| gemini-3.6-flash | 13.4–46.6s | 11 · 13 · 12 | 1.00–1.09 | 1–2/2 | 🔴 ไม่ซ้ำ |
| gemini-3.5-flash · 3.7-flash | — | — | — | — | 503 คนใช้แน่น |

🔴 **`temperature=0` ไม่ได้แปลว่าผลนิ่งอีกต่อไป** — รุ่นใหม่คิดก่อนตอบ และส่วนคิดไม่นิ่ง
3.5-flash-lite เร็วกว่าเกือบเท่าตัวก็จริง แต่ตอบไม่ซ้ำเดิมสักครั้งใน 3 รอบ
**ตัวสกัดที่ตอบไม่เหมือนเดิม ทำให้วัดก่อน-หลังไม่ได้** ซึ่งเป็นสิ่งที่ D13 ทั้งข้อตั้งอยู่บนมัน

**สิ่งที่ได้เพิ่มจาก keyword จริง ๆ** — จับประโยคที่เขียนอ้อมได้ ซึ่งคือข้อจำกัดที่ D4 บอกไว้ตรง ๆ

    "เขียนสคริปต์ให้เครื่องทำงานซ้ำ ๆ แทนคนทุกเช้า"  →  ทำท่อข้อมูลที่เดินเองทุกวัน
    "ทำ dashboard ด้วย Power BI"                      →  span เต็มประโยค ไม่ใช่แค่คำว่า "dashboard"

ทั้งคู่ได้ 11 ทักษะเท่ากันและ span ชี้กลับไปที่เอกสารได้ 11/11 ทั้งคู่ · ต่างกันที่**ตัวไหน**
(ตรงกัน 9 · เฉพาะ gemini 2 · เฉพาะ keyword 2 — keyword ยังเก็บ Linux กับ "วิเคราะห์ข้อมูล" ได้ดีกว่า)

วัดซ้ำได้: `make try-extractor P=gemini CMP=1` · เปลี่ยนรุ่นด้วย `M=<รุ่น>`

### สองเรื่องที่เสียเวลาไปแล้ว จดไว้ไม่ให้เสียซ้ำ

1. **`gemini-2.5-flash` ถูกปิดสำหรับ key ที่สร้างใหม่** — ตอบ 404 พร้อมข้อความว่า
   "no longer available to new users" ทั้งที่ยังโผล่ใน `models.list()` อยู่
   **อย่าเชื่อรายการรุ่นจาก API ว่าเรียกได้จริง**
2. **`thinking_budget=0` ทำให้รุ่นใหม่ตอบ 400** — 3.6-flash กับ 3.5-flash-lite ไม่ให้ปิดส่วนคิด
   และ 400 ของ Google ไม่บอกว่าฟิลด์ไหนผิด (`Request contains an invalid argument.` เฉย ๆ)
   ค่าเริ่มต้นของ `GEMINI_THINKING_BUDGET` จึงเป็น **-1 = ไม่ส่งค่านี้ไปเลย**
   ข้อความ error ฝั่งเราชี้ตัวแปรนี้ให้แล้ว จะได้ไม่ต้องไล่หาเองอีก

### 🔴 ยังไม่ตัดสิน — ก่อนรับ CV ของผู้ใช้จริง

ชั้นฟรีของ Google ระบุว่า**นำข้อมูลไปพัฒนาผลิตภัณฑ์ของเขา** (ชั้นเสียเงินระบุว่าไม่นำไป)
CV เป็นข้อมูลส่วนบุคคล และ 🅴 จะชวนนักศึกษาจริง ≥10 คนมาใช้ในวัน Hack Days

ที่วัดมาทั้งหมดนี้ใช้ CV สมมติในสคริปต์ ไม่ใช่ของใครจริง จึงยังไม่มีปัญหา
**แต่ก่อนเปิดให้ผู้ใช้จริงอัปโหลด ทีมต้องเลือกหนึ่งอย่าง** — เปิดชั้นเสียเงิน ·
ใช้ `local` สำหรับผู้ใช้จริง · หรือเขียนบอกไว้ในหน้าขอความยินยอมให้ชัด
ค่าเริ่มต้นของระบบยังเป็น `keyword` อยู่ ยังไม่มีอะไรถูกส่งออกไปโดยไม่ได้ตั้งใจ

### วัดซ้ำกับ CV จริง (3,615 ตัวอักษร) — ผลยืนยันการเลือกรุ่น และเจอบั๊กของ keyword

| รุ่น | เวลา | ทักษะ 3 ครั้ง | ผลซ้ำเดิม |
|---|--:|--:|:--:|
| **gemini-3.1-flash-lite** | **3.5–7.2s** | **7 · 7 · 7** | **✓ เหมือนกันทั้ง 3 ครั้ง** |
| gemini-3.5-flash-lite | 1.7–1.8s | 3 · 5 · 4 | 🔴 |
| gemini-3.6-flash | 10.7–14.6s | 4 · 2 · 3 | 🔴 |

ผลตรงกับที่วัดด้วย CV ตัวอย่าง — 3.1-flash-lite นิ่งทั้งสองเอกสาร ส่วนอีกสองตัวไม่นิ่งทั้งคู่
⚠️ เคยเจอครั้งหนึ่งใช้ 54.8 วินาที แต่วัดซ้ำ 3 ครั้งแล้วอยู่ที่ 3.5–7.2s — เป็นค่าผิดปกติ
ตอนเซิร์ฟเวอร์แน่น ไม่ใช่ค่าปกติ **แต่แปลว่าวันสาธิตต้องเผื่อกรณีช้าผิดปกติไว้ด้วย**

🔴 **บั๊กของ `keyword.py` ที่เจอเพราะได้ลองกับ CV จริง**
`re.finditer(re.escape(needle), haystack)` ไม่มีขอบเขตคำ จึง match กลางคำ — ทำซ้ำได้ทุกครั้ง:

| ข้อความใน CV | ระบบอ่านเป็นทักษะ |
|---|---|
| philo**sop**hy | P-DOC เขียน SOP / เอกสารโครงการ |
| **fea**ture engineering | ME-FEA วิเคราะห์ไฟไนต์เอลิเมนต์ |
| computer **visio**n | IE-FLOW เขียนแผนภาพด้วย Visio |
| **Pivot**ing toward MEMS | F-EXCEL ใช้ Pivot table |

span guard จับไม่ได้เพราะข้อความมีอยู่จริงในเอกสาร — **เคสเดียวกับที่ D13 เตือนไว้เป๊ะ**
และเป็นเหตุผลว่าทำไมกติกาข้อ 3 (ผู้ใช้ต้องยืนยันก่อนนับ) ตัดออกไม่ได้จริง ๆ
ยังไม่แก้ในสาขานี้ เพราะ `keyword.py` เป็นคนละงานกับการเพิ่ม provider — แยกเป็น issue ต่างหาก

**เทียบคุณภาพบน CV จริง** (ตรงกันแค่ 3 จาก 7 vs 12)
· gemini พลาดแบบ "ตีความเกิน" — `"ARMv7 Assembly"` ถูกนับเป็นทักษะภาษา C ซึ่งคนละภาษา
· keyword พลาดแบบ "จับคำผิดตัว" 4 อันข้างบน ซึ่งอ่านแล้วไม่เป็นเหตุเป็นผลเลยสำหรับผู้ใช้
· ทั้งคู่ผ่าน span guard 100% — **ยืนยันว่า guard กันการแต่งข้อความได้ แต่กันการตีความผิดไม่ได้**

### CV ตัวอย่างที่ออกแบบมาเพื่อวัด — และบั๊กของ keyword ที่โผล่เพิ่มเป็นภาษาไทย

`~/Desktop/mock_cv_siit.pdf` (1,500 ตัวอักษร) เขียนขึ้นให้ทุกบรรทัดวัดอะไรอย่างหนึ่ง
ไม่ใช่ CV สวย ๆ เฉย ๆ · **ไม่ใช่ข้อมูลของคนจริง** จึงส่งเข้าชั้นฟรีได้ไม่ต้องกังวล

| กลุ่ม | ใส่มาเพื่อ | ผล |
|---|---|---|
| A เขียนคำตรง ๆ | ทั้งคู่ควรจับได้ | ✓ ทั้งคู่ |
| B เขียนอ้อม ไม่มีคำตรง | keyword ควรจับไม่ได้ · LLM ควรจับได้ | gemini ได้ 2 จาก 5 · keyword 0 |
| C คำสำคัญซ้อนกลางคำอื่น | keyword จะจับผิด | 🔴 keyword ผิด **5 อัน** · gemini 0 |
| D เขียนว่า "ยังไม่เคยทำ" | ห้ามนับเป็นทักษะ | 🔴 keyword นับ Tableau · gemini ไม่นับ |

**ผลรวม gemini 11 ทักษะ · keyword 16 ทักษะ** — แต่ของ keyword มีที่ผิดชัด ๆ 6 อัน (~40%)

🔴 **บั๊กเดียวกันเกิดกับภาษาไทยด้วย** — ข้อนี้ไม่ได้ตั้งใจใส่มา แต่โผล่เอง

    "ห้องปฏิบัติการระบบอัตโนมัติ"  →  F-MEASURE "วัดของจริงและจดผลอย่างเป็นระบบ"
                                       เพราะคำว่า "ปฏิบัติการ" ซ้อนอยู่ในคำว่า "ห้องปฏิบัติการ"

แปลว่าการแก้ด้วย `\b` ของ regex ไม่พอ และไม่ตรงประเด็นด้วย — ภาษาไทยไม่มีเว้นวรรคระหว่างคำ
**ใครแก้บั๊กนี้ต้องทดสอบทั้งสองภาษา** ไม่ใช่แก้ฝั่งอังกฤษแล้วคิดว่าจบ

**กลุ่ม D คือหลักฐานตรง ๆ ของกติกาข้อ 3** — CV เขียนว่า *"เคยเห็นรุ่นพี่ใช้ Tableau ทำรายงาน
แต่ตัวเองยังไม่ได้ลอง"* แล้ว keyword นับเป็นทักษะ "ทำภาพข้อมูลที่คนดูแล้วตัดสินใจได้ทันที"
span ชี้กลับไปได้ถูกต้อง 100% แต่ความหมายกลับหัวกลับหาง — **guard กันเรื่องนี้ไม่ได้ คนต้องกัน**

**สิ่งที่ gemini ยังพลาด อย่าขายเกิน** — ประโยคอ้อมสามอันนี้มันจับไม่ได้:
· *"ทำให้สองระบบนั้นส่งข้อมูลหากันเองได้"* · *"ให้เครื่องดึงยอดของเสียมาสรุปเองทุกเช้า"*
· *"ไปนั่งคุยกับหัวหน้าไลน์...แล้วเรียบเรียงเป็นข้อกำหนด"*
ได้ 2 จาก 5 ของกลุ่ม B เท่านั้น — ดีกว่า keyword (ซึ่งได้ 0) แต่ไม่ใช่ว่าอ่านได้ครบ

### แล้วทำไมไม่ใช้ Flash ตัวเต็ม — วัดแล้ว ไม่ใช่เพราะไม่ได้ลอง

คำถามที่ถูกต้อง เพราะ Flash **สกัดได้ดีกว่าจริง** วัดกับ `mock_cv_siit.pdf` ตัวเดียวกัน:

| | เรียกสำเร็จ | เวลา | จับประโยคอ้อม DA-PIPE | ติดกับดัก | นับ Tableau ผิด |
|---|:--:|--:|:--:|:--:|:--:|
| gemini-3.5-flash | **1/3** 🔴 | 21.6s | **✓** | 0 | ไม่ |
| gemini-3.1-flash-lite | 3/3 | 9–13s | ✗ | 0 | ไม่ |

Flash จับ *"เขียนโปรแกรมให้เครื่องดึงยอดของเสียมาสรุปเองทุกเช้าตอนหกโมง"* → DA-PIPE ได้
ซึ่ง flash-lite จับไม่ได้ทั้ง 3 ครั้ง — **นั่นคือความสามารถที่เราจ่ายเงินซื้อ LLM มาเพื่อมันเลย**

🔴 **แต่ 2 ใน 3 ครั้ง Flash ตอบ 503 (เซิร์ฟเวอร์แน่น)** และเจอตลอดทั้งวัน ไม่ใช่จังหวะเดียว
`gemini-3.7-flash` ก็เหมือนกัน · `gemini-3.6-flash` เรียกติดแต่ผลไม่ซ้ำเดิม (ดูตารางข้างบน)

**นี่ไม่ใช่การเลือกระหว่างความแม่น แต่เป็นข้อจำกัดของชั้นฟรี**

| ถ้าทีมเลือก | ควรใช้ |
|---|---|
| อยู่ชั้นฟรี · ต้องการให้สาธิตไม่พลาดกลางเวที | `gemini-3.1-flash-lite` (ตอบทุกครั้ง แลกกับจับประโยคอ้อมได้น้อยกว่า) |
| เปิดชั้นเสียเงิน (ซึ่งต้องทำอยู่แล้วเพราะเรื่องข้อมูลส่วนบุคคล) | **วัด Flash ใหม่ก่อนตัดสิน** — ถ้า 503 หายไป Flash ควรเป็นตัวเลือกหลัก |

⚠️ **ห้ามสรุปจากตารางนี้ว่า Flash ใช้ไม่ได้** — 503 เป็นเรื่องโควตา ไม่ใช่ความสามารถของรุ่น
วันที่เปิดชั้นเสียเงินแล้วต้องวัดซ้ำ: `make try-extractor P=gemini M=gemini-3.5-flash CMP=1`

### เจอ 503 แล้วลองใหม่ให้เอง — 3 ครั้ง หน่วงเพิ่มขึ้นทุกรอบ

503 ไม่ใช่ความผิดของเราและมักหายเองในไม่กี่วินาที ปล่อยให้ผู้ใช้เห็น error ทั้งที่กดใหม่
แล้วผ่าน = โยนภาระให้ผู้ใช้แก้ปัญหาของ Google · ระบบจึงลองใหม่ให้เอง

    ครั้งที่ 1 → ล้ม → หน่วง 2 วินาที → ครั้งที่ 2 → ล้ม → หน่วง 4 วินาที → ครั้งที่ 3
    ช้าสุดที่เพิ่มขึ้น 6 วินาที · ยังเร็วกว่า local ที่ D13 วัดไว้ 78 วินาทีมาก

🔒 **ลองใหม่เฉพาะ 503 เท่านั้น** — มีเทสต์คุมทีละกรณี
· `429` โควตาหมด · ยิงซ้ำยิ่งแย่
· `400` คำขอผิดตั้งแต่แรก · `404` รุ่นเรียกไม่ได้ · `key` ผิด — ยิงกี่รอบก็ผิดเหมือนเดิม
ทั้งสี่กรณีต้องเลิกตั้งแต่ครั้งแรก ไม่ให้ผู้ใช้รอฟรี

ข้อความตอนยอมแพ้บอกจำนวนครั้งที่ลองไปแล้ว ผู้ใช้จะได้ไม่กดซ้ำเองโดยเปล่าประโยชน์

⚠️ **ยังไม่ได้พิสูจน์กับ 503 จริง** — ตอนวัด (15 ส.ค. เย็น) ยิง `gemini-3.5-flash` 5 ครั้ง
ผ่านหมดตั้งแต่รอบแรกทั้ง 5 ครั้ง การลองใหม่จึงยังไม่เคยถูกใช้งานจริง
เทียบกับเช้าวันเดียวกันที่รุ่นเดียวกันได้ 1/3 — **ยืนยันว่า 503 ขึ้นกับช่วงเวลา ไม่ใช่ค่าคงที่**
ตรรกะคุมด้วยเทสต์ครบแล้ว แต่ใครเจอ 503 จริงหลังจากนี้ ช่วยยืนยันว่ามันกู้คืนได้จริงด้วย

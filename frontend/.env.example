# คัดลอกเป็น .env.local แล้วแก้ค่า — 🔴 .env* ห้ามเข้า repo

# ที่อยู่ของ API รวม /api ต่อท้าย
# 🔴 ตัวแปรที่ขึ้นต้นด้วย NEXT_PUBLIC_ ถูกฝังลงไปตอน build ไม่ใช่ตอนรัน
#    เปลี่ยนค่านี้แล้วต้อง build ใหม่เสมอ ไม่ใช่แค่รีสตาร์ต
NEXT_PUBLIC_API_BASE=http://localhost:8000/api
